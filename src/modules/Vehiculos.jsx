import { useEffect,useState } from 'react';
import { supabase } from '../lib/supabase';
export default function Vehiculos(){const [rows,setRows]=useState([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');async function load(){setLoading(true);const {data,error}=await supabase.from('vehiculos').select('*').order('created_at',{ascending:false});if(error)setError(error.message);setRows(data||[]);setLoading(false)}useEffect(()=>{load()},[]);return <div className="panel"><div className="module-head"><div><h2>Vehículos</h2><p className="muted">Vehículos visibles según RLS.</p></div><button className="primary compact" onClick={load}>Actualizar</button></div>{error&&<div className="error">{error}</div>}{loading?<p className="muted">Cargando…</p>:rows.length===0?<p className="muted">No hay vehículos todavía.</p>:<div className="table-wrap"><table><thead><tr><th>Matrícula</th><th>Marca</th><th>Modelo</th><th>Año</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.matricula||r.placa||'—'}</td><td>{r.marca||'—'}</td><td>{r.modelo||'—'}</td><td>{r.anio||r.año||'—'}</td></tr>)}</tbody></table></div>}</div>}
