import { useEffect, useState } from 'react';
import { localApi } from '../lib/api';
export default function VehiculosLocal(){const [rows,setRows]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');async function load(){setLoading(true);try{setRows(await localApi.vehiculos());setError('')}catch(e){setError(e.message)}finally{setLoading(false)}}useEffect(()=>{load()},[]);return <div className="panel"><div className="module-head"><div><h2>Vehículos</h2><p className="muted">Datos de la base local del taller.</p></div><button className="primary compact" onClick={load}>Actualizar</button></div>{error&&<div className="error">{error}</div>}{loading?<p className="muted">Cargando…</p>:rows.length===0?<p className="muted">No hay vehículos todavía.</p>:<div className="table-wrap"><table><thead><tr><th>Matrícula</th><th>Marca</th><th>Modelo</th><th>Año</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.matricula||'—'}</td><td>{r.marca||'—'}</td><td>{r.modelo||'—'}</td><td>{r.anio||'—'}</td></tr>)}</tbody></table></div>}</div>}
