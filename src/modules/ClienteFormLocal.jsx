import { useState } from 'react';
import { localApi } from '../lib/api';
export default function ClienteFormLocal({onCreated}){const [form,setForm]=useState({nombre:'',apellidos:'',telefono:'',email:''});const [busy,setBusy]=useState(false);const [error,setError]=useState('');const change=e=>setForm({...form,[e.target.name]:e.target.value});async function submit(e){e.preventDefault();setBusy(true);setError('');try{const row=await localApi.createCliente(form);setForm({nombre:'',apellidos:'',telefono:'',email:''});onCreated?.(row)}catch(e){setError(e.message)}finally{setBusy(false)}}return <form className="local-form" onSubmit={submit}><div className="form-grid"><label>Nombre<input name="nombre" value={form.nombre} onChange={change} required/></label><label>Apellidos<input name="apellidos" value={form.apellidos} onChange={change}/></label><label>Teléfono<input name="telefono" value={form.telefono} onChange={change}/></label><label>Email<input type="email" name="email" value={form.email} onChange={change}/></label></div>{error&&<div className="error">{error}</div>}<button className="primary compact" disabled={busy}>{busy?'Guardando…':'Guardar cliente'}</button></form>}
