import {useEffect,useMemo,useState} from 'react';
import {localApi} from '../lib/api';
const empty={cliente_id:'',vehiculo_id:'',numero_ot:'',fecha_entrada:'',fecha_prevista_entrega:'',fecha_entrega:'',estado:'presupuesto',prioridad:'normal',asesor_id:'',mecanico_id:'',kilometraje_entrada:'',nivel_combustible:'',llaves_entregadas:false,motivo_entrada:'',averia_comunicada:'',diagnostico:'',trabajo_solicitado:'',danos_observaciones:'',accesorios:'',descripcion:'',observaciones:''};
export default function OrdenesLocal(){const[rows,setRows]=useState([]),[clientes,setClientes]=useState([]),[vehiculos,setVehiculos]=useState([]),[usuarios,setUsuarios]=useState([]),[search,setSearch]=useState(''),[selected,setSelected]=useState(null),[form,setForm]=useState(null),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[error,setError]=useState('');async function load(){setLoading(true);setError('');try{const[o,c,v,u]=await Promise.all([localApi.ordenes(),localApi.clientes(),localApi.vehiculos(),localApi.usuarios()]);setRows(o);setClientes(c);setVehiculos(v);setUsuarios(u)}catch(e){setError(e.message)}finally{setLoading(false)}}useEffect(()=>{load()},[]);const filtered=useMemo(()=>{const q=search.trim().toLowerCase();return q?rows.filter(r=>{const c=clientes.find(x=>x.id===r.cliente_id),v=vehiculos.find(x=>x.id===r.vehiculo_id);return[r.numero_ot,r.descripcion,r.estado,c?.nombre,c?.apellidos,v?.matricula,v?.marca,v?.modelo].some(x=>String(x||'').toLowerCase().includes(q))}):rows},[rows,search,clientes,vehiculos]);const open=r=>{setSelected(r);setForm({...empty,...r,llaves_entregadas:!!r.llaves_entregadas,fecha_entrada:r.fecha_entrada||'',fecha_prevista_entrega:r.fecha_prevista_entrega||'',fecha_entrega:r.fecha_entrega||''})};const newOrder=()=>{setSelected({id:null});setForm({...empty,fecha_entrada:new Date().toISOString().slice(0,16)})};const change=e=>{const{name,value,type,checked}=e.target;setForm({...form,[name]:type==='checkbox'?checked:value})};async function save(e){e.preventDefault();setBusy(true);setError('');try{let saved;if(selected?.id)saved=await localApi.updateOrden(selected.id,form);else saved=await localApi.createOrden(form);setRows(rs=>selected?.id?rs.map(r=>r.id===saved.id?saved:r):[saved,...rs]);setSelected(saved);setForm({...empty,...saved,llaves_entregadas:!!saved.llaves_entregadas})}catch(e){setError(e.message)}finally{setBusy(false)}}const available=vehiculos.filter(v=>!form?.cliente_id||v.cliente_id===form.cliente_id);return <div className="panel"><div className="module-head"><div><h2>Órdenes de trabajo</h2><p className="muted">Recepción, planificación y seguimiento de la reparación.</p></div><div><button className="primary compact" onClick={newOrder}>+ Nueva OT</button> <button className="compact" onClick={load}>Actualizar</button></div></div><input placeholder="Buscar por N.º OT, cliente, matrícula, descripción o estado…" value={search} onChange={e=>setSearch(e.target.value)}/>{error&&<div className="error">{error}</div>}{loading?<p className="muted">Cargando…</p>:<div className="table-wrap"><table><thead><tr><th>N.º OT</th><th>Cliente</th><th>Vehículo</th><th>Descripción</th><th>Estado</th><th></th></tr></thead><tbody>{filtered.map(r=>{const c=clientes.find(x=>x.id===r.cliente_id),v=vehiculos.find(x=>x.id===r.vehiculo_id);return <tr key={r.id}><td>{r.numero_ot||'—'}</td><td>{[c?.nombre,c?.apellidos].filter(Boolean).join(' ')||'—'}</td><td>{v?.matricula||'—'}</td><td>{r.descripcion||'—'}</td><td>{r.estado||'abierta'}</td><td><button className="primary compact" onClick={()=>open(r)}>Abrir / editar</button></td></tr>})}</tbody></table>{filtered.length===0&&<p className="muted">No se encontraron órdenes.</p>}</div>}{form&&<div className="panel"><div className="module-head"><div><h2>{selected?.id?'Ficha de OT':'Nueva orden de trabajo'}</h2><p className="muted">Datos de recepción del vehículo</p></div><button className="compact" onClick={()=>{setSelected(null);setForm(null)}}>Cerrar</button></div><form className="local-form" onSubmit={save}><h3>Recepción</h3><div className="form-grid"><label>Cliente<select name="cliente_id" value={form.cliente_id} onChange={e=>setForm({...form,cliente_id:e.target.value,vehiculo_id:''})} required><option value="">Seleccionar…</option>{clientes.map(c=><option key={c.id} value={c.id}>{[c.nombre,c.apellidos].filter(Boolean).join(' ')}</option>)}</select></label><label>Vehículo<select name="vehiculo_id" value={form.vehiculo_id} onChange={change} required><option value="">Seleccionar…</option>{available.map(v=><option key={v.id} value={v.id}>{v.matricula} · {v.marca||''} {v.modelo||''}</option>)}</select></label><label>N.º OT<input name="numero_ot" value={form.numero_ot} onChange={change}/></label><label>Estado<select name="estado" value={form.estado} onChange={change}><option value="presupuesto">Presupuesto</option><option value="pendiente_autorizacion">Pendiente de autorización</option><option value="autorizada">Autorizada</option><option value="en_espera">En espera</option><option value="en_reparacion">En reparación</option><option value="terminada">Terminada</option><option value="entregada">Entregada</option><option value="cancelada">Cancelada</option></select></label><label>Prioridad<select name="prioridad" value={form.prioridad} onChange={change}><option value="normal">Normal</option><option value="alta">Alta</option><option value="urgente">Urgente</option></select></label><label>Fecha/hora entrada<input type="datetime-local" name="fecha_entrada" value={form.fecha_entrada||''} onChange={change}/></label><label>Entrega prevista<input type="datetime-local" name="fecha_prevista_entrega" value={form.fecha_prevista_entrega||''} onChange={change}/></label><label>Entrega real<input type="datetime-local" name="fecha_entrega" value={form.fecha_entrega||''} onChange={change}/></label><label>Kilometraje entrada<input type="number" min="0" name="kilometraje_entrada" value={form.kilometraje_entrada||''} onChange={change}/></label><label>Nivel combustible<select name="nivel_combustible" value={form.nivel_combustible||''} onChange={change}><option value="">Sin indicar</option><option value="reserva">Reserva</option><option value="1/4">1/4</option><option value="1/2">1/2</option><option value="3/4">3/4</option><option value="lleno">Lleno</option></select></label><label className="check"><input type="checkbox" name="llaves_entregadas" checked={!!form.llaves_entregadas} onChange={change}/> Llaves entregadas</label></div><h3>Trabajo y diagnóstico</h3><div className="form-grid"><label className="wide">Motivo de entrada<textarea name="motivo_entrada" value={form.motivo_entrada||''} onChange={change}/></label><label className="wide">Avería comunicada por el cliente<textarea name="averia_comunicada" value={form.averia_comunicada||''} onChange={change}/></label><label className="wide">Trabajo solicitado<textarea name="trabajo_solicitado" value={form.trabajo_solicitado||''} onChange={change}/></label><label className="wide">Diagnóstico<textarea name="diagnostico" value={form.diagnostico||''} onChange={change}/></label><label className="wide">Daños / observaciones exteriores<textarea name="danos_observaciones" value={form.danos_observaciones||''} onChange={change}/></label><label className="wide">Accesorios dejados en el vehículo<textarea name="accesorios" value={form.accesorios||''} onChange={change}/></label><label className="wide">Descripción resumida<input name="descripcion" value={form.descripcion||''} onChange={change} required/></label><label className="wide">Observaciones internas<textarea name="observaciones" value={form.observaciones||''} onChange={change}/></label></div><h3>Responsables</h3><div className="form-grid"><label>Asesor<select name="asesor_id" value={form.asesor_id||''} onChange={change}><option value="">Sin asignar</option>{usuarios.map(u=><option key={u.id} value={u.id}>{[u.nombre,u.apellido].filter(Boolean).join(' ')}</option>)}</select></label><label>Mecánico<select name="mecanico_id" value={form.mecanico_id||''} onChange={change}><option value="">Sin asignar</option>{usuarios.map(u=><option key={u.id} value={u.id}>{[u.nombre,u.apellido].filter(Boolean).join(' ')}</option>)}</select></label></div>{error&&<div className="error">{error}</div>}<button className="primary" disabled={busy}>{busy?'Guardando…':'Guardar OT'}</button></form></div>}</div>}
